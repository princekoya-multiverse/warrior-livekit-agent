# Frontend environment template (copy to .env.local)
# URL of the token server endpoint the browser calls to get a LiveKit token.
# In local dev, leave as /api/token (proxied by Vite to the token server).
VITE_TOKEN_ENDPOINT=/api/token
